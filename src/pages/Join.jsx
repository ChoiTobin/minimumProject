import React from 'react';
import styled from "styled-components";
import Layout from '../components/Layout';

const Join = () => {
    return (
        <Layout>
            <div>Join</div>
        </Layout>
    );
};

export default Join;

